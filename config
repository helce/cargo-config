[patch]

[patch.crates-io]
# I don't have enough new kernel and glibc to test later versions. But you can use them. Just set last tag instead.
libc = { git = 'https://github.com/helce/libc', tag = '0.2.133' }
